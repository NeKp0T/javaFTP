package com.example.ftp.model;

/**
 * An interface representing a file in a filesystem.
 */
public interface FileView {
    /**
     * A getter for a file's name.
     * @return file's name
     */
    String getName();
}
